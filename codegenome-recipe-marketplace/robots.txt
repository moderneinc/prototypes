# codegenomeproject.org
# Homepage-only indexing. Crawling stays open so search engines can reach the
# app routes and honor the X-Robots-Tag: noindex header set on everything except
# "/" (see nginx.conf). Do NOT Disallow the app routes here, or crawlers can't
# fetch them to see the noindex.

User-agent: *
Allow: /

Sitemap: https://codegenomeproject.org/sitemap.xml
